spring:
  application:
    name: zt-framework-rbac-system
  datasource:
    druid:
      driverClassName: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/zt_user_center?useUnicode=true&characterEncoding=utf8&useSSL=false&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Shanghai
      username: root
      password: root

mybatis:
  mapper-locations: classpath*:/mapper/**.xml

logging:
  level:
    com:
      zhangzhuorui:
        framework: trace
